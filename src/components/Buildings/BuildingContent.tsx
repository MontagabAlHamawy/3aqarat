import React from 'react'
import BuildingError from '../error/BuildingError';

export default function BuildingContent(property: any) {
    console.log("property: ", property);

    return (
        <BuildingError/>
    )
}
